# Language Preference Command

This command is optional.

The project-level rule now lives in:

- `.cursor/rules/language-preference.mdc`

Because that rule uses `alwaysApply: true`, you usually do not need to run this command manually.
